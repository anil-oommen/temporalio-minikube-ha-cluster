server:
  port: 8080

app:
  config:
    temporal:
      tio1:
        name: tio
        target: tio-temporal-frontend:7233
        namespace: fastns
        task-queue: "SIMPLE_TASK_QUEUE"
        use-search-attribute: true

